Secomd Commit

create new repo github commands
example:
git remote set-url origin https://github.com/your-username/your-repo.git
git branch -M main
git push -u origin main

After that (future updates)
git add .
git commit -m "your message"
git push
